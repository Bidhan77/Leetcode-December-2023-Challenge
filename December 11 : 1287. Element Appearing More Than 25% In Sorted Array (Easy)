class Solution {
    public int findSpecialInteger(int[] arr) 
    {
        var x = arr.length/4;
        for(var i=0;i<arr.length;i++)
            if(arr[i]==arr[i+x])
                return arr[i];
        return -1;
    }
}
